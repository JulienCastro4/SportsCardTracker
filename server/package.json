{
  "name": "sports-card-tracker-server",
  "version": "1.0.0",
  "description": "Backend server for sports card tracker application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "migrate": "node -e \"require('child_process').execSync('psql -U postgres -d mycardcollectiondb -f db/init.sql', { stdio: 'inherit' })\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-oauth2-jwt-bearer": "^1.5.1",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.10.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
